require("nvchad.configs.lspconfig").defaults()

-- LSP servers will be automatically set up by mason-lspconfig
-- No need to manually configure here - mason-lspconfig handles it

-- Optional: Add custom configurations per server below if needed
-- Example:
-- local lspconfig = require "lspconfig"
-- lspconfig.pyright.setup {
--   settings = {
--     python = {
--       analysis = {
--         typeCheckingMode = "basic",
--       },
--     },
--   },
-- }

